modules/logs/**/
.cfg
node_modules
.idea
.luarc.jsonc

# Bitirim: web/build BILEREK commit edilir.
# init.lua acilista 'web/build/index.html' arar; yoksa kaynak hic baslamaz.
# Sunucuda (Ubuntu) Bun/Node kurulu olmadigi icin derleme orada yapilamaz —
# lokalde 'cd web && bun run build' calistirip cikti ile birlikte commit ediyoruz.
